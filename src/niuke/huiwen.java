package niuke;

import java.util.*;

/**
 * Created by mazhi on 2017/3/23.
 */
public class huiwen {
    public static void main(String[] args){
       /* Scanner in = new Scanner(System.in);
        int N= 0;
        N = in.nextInt();
        int[][] tmp = new int[N][26];
        int row = 0;
        while(in.hasNextLine() && row < N){
            String s = in.nextLine().trim();
            for(int i = 0 ; i < s.length() ; i++){
                tmp[row][s.charAt(i) - 'a'] += 1;
            }
            row++;
        }
        Map<int[],Integer> test = new HashMap<>();
        for(int i = 0; i < tmp.length ; i++){
            test.put(tmp[i],1);
        }
        System.out.println(tmp[1].equals(tmp[2]));*/
        int[] a = {1};
        int[] b = {1};
        System.out.println(a.equals(b));
        /*System.out.println(test.size());*/
    }
}
